// URL Checking via PHP

<?php
  $url = "https://www.geeksforgeeks.org";
  
  $headers = @get_headers($url);
  
  if($headers && strpos( $headers[0], '200')) {
      $status = "URL Exist";
  }
  else {
      $status = "URL Doesn't Exist";
  }
  
  echo($status);
  
?>